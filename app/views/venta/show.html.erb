<style type="text/css">
  p{
    color:white;
   }
   td{
    background-color: white;
   }
   thead{
    color:white;
   }
   strong{
    color:white;
   }
     label{
    color:white;
   }


</style>
<h1><strong>Datos de la Venta</strong></h1>
<p id="notice"><%= notice %></p>
<p>
	<strong>Cliente:</strong>
  <%if Cliente.find_by(id: @ventum.cliente_id) != nil %>
        <%= (Cliente.find_by(id: @ventum.cliente_id)).nombre %>
        <%= (Cliente.find_by(id: @ventum.cliente_id)).apellido %>
  <%else%>
        N/A
  <%end %>
</p>
<p>
  <strong>Costo:</strong>
  <%= @ventum.Costo %>
</p>
<p>
  <strong>ID:</strong>
  <%= @ventum.id %>
</p>
<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    

<div class="container">
    <div class="alert alert-danger" role="alert">
      <font color="black"><h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2></font>
      <% @list.errors.full_messages.each do |message| %>
        <font color="black"><li><%= message %></li></font>
      <% end %>
      </div>
    </div>


      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.hidden_field :ventum_id, :value => @ventum.id %>
  </div>
  <div class="field">
    <%= f.label :producto %><br>
   
    <%= f.collection_select(:producto, Prodcuto.all, :nombre, :nombre) %>
  </div>
   <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% if @ventum.lists.count > 0%>
    <strong>Lista de productos</strong>
    <br>
    <table>
      <tr>
        <td>Producto</td>
        <td>costo </td>
        <td>Unidades</td>
      </tr>
    <% @ventum.lists.each do |com|%>      
      <tr>
        <td><%=com.producto %></td>
        <td><%=com.costo %> BS.</td>
        <td><%=com.cantidad %></td>
        <td><%= link_to 'Destroy', com, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%end%>
    </table>
  <%end%>


<%= link_to 'Edit', edit_ventum_path(@ventum), class: "btn btn-warning" %> 
<%= link_to 'Back', venta_path , class: "btn btn-primary"%>
