<style type="text/css">
   label{
    color:white;
   }
   h1{
     color: white;
   }
</style>
<%= form_for(@atencion) do |f| %>
  <% if @atencion.errors.any? %>
    <div class="container">
    <div class="alert alert-danger" role="alert">
      <font color="black"><h2><%= pluralize(@atencion.errors.count, "error") %> prohibited this atencion from being saved:</h2></font>
      <% @atencion.errors.full_messages.each do |message| %>
         <font color="black"><li><%= message %></li></font>
      <% end %>
    </div>
  </div>
  <% end %>

  <div class="field, container">
    <table class="table">
      <tr>
        <td><%= f.label :Dueño %></td>
        <td><%= f.collection_select(:cliente_id, Cliente.all, :id, :ci)  %></td>
      </tr>
      <%= hidden_field_tag :mascotum_id, nil %>
      <tr>
        <td><%= f.label :Veterinario %></td>
        <td><%= f.collection_select(:veterinario_id, Veterinario.all, :id, :ci) %></td>
      </tr>
      <tr>
        <td><%= f.label :fecha %></td>
        <td><%= f.date_select :fecha %></td>
      </tr>

      <tr>
        <td><%= f.label :anamnesis %></td>
        <td><%= f.text_area :anamnesis, {:id => "lol"} %></td>
      </tr>
      <tr>
        <td><%= f.label :temperatura %></td>
        <td><%= f.number_field :temperatura %></td>
      </tr>
      <tr>
        <td><%= f.label :mucosas %></td>
        <td><%= f.text_area :mucosas %></td>
      </tr>
      <tr>
        <td><%= f.label :palpacion %></td>
        <td><%= f.text_area :palpacion %></td>
      </tr>
      <tr>
        <td><%= f.label :diagnostico %></td>
        <td><%= f.text_area :diagnostico %></td>
      </tr>
      <tr>
        <td><%= f.label :tratamiento %></td>
        <td><%= f.text_area :tratamiento %></td>
      </tr>
    </table>
  </div>
  <div class="actions">
    <%= f.submit 'Guardar', class:"big ui button, ui inverted olive button"  %>
  </div>
<% end %>
